{
  "name": "es6-todo-app",
  "version": "1.0.0",
  "description": "A native ES6 TODO App",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/index.js"
  },
  "keywords": [
    "ES6",
    "TODO"
  ],
  "author": "Miguel Felipe Calo",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.0",
    "rimraf": "^2.6.1"
  },
  "dependencies": {
    "express": "^4.16.3"
  }
}
